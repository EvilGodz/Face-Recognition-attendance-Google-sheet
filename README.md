Install Required Libraries:

	pip install oauth2client
	pip install gspread
	pip install face_recognition
	pip install opencv-python
	pip install pickle-mixin
	pip install smtplib
	

Update enroll.py:
```
photo_folder = 'path/to/Face Recognition google sheet/known faces photos/'
facial_encodings_folder = 'path/to/Face Recognition google sheet/known face encodings/'
 ```
Register Faces in regist.py

Run main.py
